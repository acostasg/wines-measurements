# Authentication

The authentication system is based on JWT (Json web token).

You will have to request your username and password to obtain a token that will give you access to different endpoints.

<aside class="success">
All request needs a <code>Authorization: Bearer &lt;TOKEN&gt;</code> token as a header.
</aside>
